You are an Apache Airflow expert. Write a Python function called transform_covid_data(**context) for a PythonOperator.

The function should:
1. Pull clean CSV path from XCom
2. Load with pandas, sort by iso_code + date
3. Compute these features PER COUNTRY (use groupby + transform):
- new_cases_7day_avg: 7-day rolling average of new_cases
- new_deaths_7day_avg: 7-day rolling average of new_deaths
- cases_per_million: (total_cases / population) * 1,000,000
- deaths_per_million: (total_deaths / population) * 1,000,000
- case_fatality_rate: (total_deaths / total_cases) * 100, handle division by zero
4. Round all computed float columns to 2 decimal places
5. Save to /tmp/covid_transformed.csv and push path to XCom
