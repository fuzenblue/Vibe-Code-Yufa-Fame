You are an Apache Airflow expert. Write a Python function called load_happiness_model(**context) for a PythonOperator.

The function should:
1. Pull clean CSV path from XCom
2. Load with pandas and connect to MySQL via SQLAlchemy
3. Create a dimensional model:
- dim_country: country_id (PK), country_name, region (NULL for now)
- dim_year: year_id (PK), year
- fact_happiness: fact_id, country_id (FK), year_id (FK), happiness_score, gdp_per_capita, social_support, life_expectancy, freedom, generosity, corruption, happiness_rank (computed rank per year using pandas rank())
4. Compute rank within each year group before loading
5. Write all tables to MySQL with chunked inserts
6. Log final row counts
