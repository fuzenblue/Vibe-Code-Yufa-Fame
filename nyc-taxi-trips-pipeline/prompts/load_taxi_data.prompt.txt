Role: Act as a Senior Data Engineer and Apache Airflow Expert.
Task: Write a production-grade Python function named load_taxi_model(**context) for use in an Airflow PythonOperator.
Functional Requirements:
1. Extraction: Pull the transformed CSV path from XCom using task_ids='transform_data' and key='transformed_csv_path'. Read the CSV using pandas with dtype=str to prevent initial type-casting issues.
2. Connection: Establish a SQLAlchemy engine using pymysql. Credentials must be fetched from Airflow Variables: MYSQL_HOST, MYSQL_USER, MYSQL_PASS, and MYSQL_DB. Implement a "fast-fail" connectivity check using pool_pre_ping=True and a dummy SELECT 1 execution.
3. Star Schema Modeling:
   * dim_time: Unique rows based on pickup_datetime. Include hour, day, month, and is_weekend (boolean).
   * dim_payment: Unique rows for payment_type.
   * fact_trips: Grain is one row per trip. It must map surrogate keys (IDs) from the dimensions above. Include measures: fare_amount, trip_distance, trip_duration_minutes, speed_mph, fare_per_mile, and passenger_count.
4. Safe Calculations: Implement a helper function for division-by-zero protection when calculating speed_mph and fare_per_mile. Use $0.0$ as a sentinel value for invalid divisions.
5. Loading: Use df.to_sql with if_exists='replace', chunksize=1000, and method='multi' for optimized MySQL inserts. Ensure dimensions are loaded before the fact table.
6. Cleanup & Logging: Ensure engine.dispose() is called in a finally block. Log detailed row counts for each table and a final grand total summary in a structured format.
Code Style Requirements:
* Use Type Hints and from __future__ import annotations.
* Follow a Modular Structure by breaking the logic into internal helper functions (e.g., _build_dim_time, _load_table).
* Include comprehensive Docstrings and professional Logging (using logging.getLogger(__name__)).
* Ensure the code is robust, handles NaT or unparseable dates gracefully, and follows PEP 8 standards.